import React from 'react'
import { Card } from 'antd'

interface IProps {
  loading: boolean
}

const Settings = (props: IProps) => {

  return (
    <Card>
      Settings
    </Card>
  )
}

export default Settings
