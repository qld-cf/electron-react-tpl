import React from 'react'
import { Card } from 'antd'

interface IProps {
  loading: boolean
}

const Edge = (props: IProps) => {

  return (
    <Card>
      Edge
    </Card>
  )
}

export default Edge
